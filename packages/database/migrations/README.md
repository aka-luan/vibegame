# Migrations

Migrations in this directory are forward-only SQL generated by Drizzle and
reviewed before use. Run `pnpm db:migrate` against the disposable local
PostgreSQL instance. Never rewrite a migration already used outside that local
environment; add a new migration for every schema change.
