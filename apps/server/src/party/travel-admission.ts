/**
 * A ticket must be consumed before its own expiry. Once consumption begins,
 * this bounded grace keeps the already-promised party seat alive while the
 * destination room finishes fallible equipment and quest preparation.
 */
export const PARTY_JOIN_COMPLETION_GRACE_MS = 5_000;
